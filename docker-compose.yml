version: "3.3"
services:
  database:
    image: postgres:latest
    restart: always
    ports:
      - 5432:5432
    volumes:
      - ./.database_data:/var/lib/postgresql
      - ./db/structure.sql:/docker-entrypoint-initdb.d/structure.sql
    environment:
      - POSTGRES_USER=${MEMORY_TEST_DATABASE_USER}
      - POSTGRES_PASSWORD=${MEMORY_TEST_DATABASE_PWD}
      - POSTGRES_DB=${MEMORY_TEST_DATABASE}

  backend:
    build:
      context: ./back
    volumes:
      - ./back:/app
      - /app/node_modules
    working_dir: /app
    command: yarn dev
    environment:
      - MEMORY_TEST_DATABASE_URL=${MEMORY_TEST_DATABASE_URL}
    ports:
      - 3000:3000
    depends_on:
      - database

  frontend:
    build:
      context: ./front
    volumes:
      - ./front:/app
      - /app/node_modules
    working_dir: /app
    command: yarn start
    ports:
      - 4200:4200
    depends_on:
      - backend
